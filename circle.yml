machine:
  environment:
    LC_CTYPE: en_US.UTF-8

dependencies:
  override:
    - sudo gem install xcpretty

test:
  override:
    - rake cidebug
    - make test && cp build/test-report.xml $CIRCLE_TEST_REPORTS
